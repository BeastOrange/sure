---
zh-CN:
  activerecord:
    errors:
      models:
        rule:
          duplicate_actions: 规则不能有重复动作 %{types}
          min_actions: 至少必须有一个动作
          nested_conditions: 复合条件不能嵌套
  rule:
    conditions:
      unsupported_label: "不支持（%{type}）"
